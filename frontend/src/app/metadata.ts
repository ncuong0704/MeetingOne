import { Metadata } from 'next'
import { BRAND_NAME } from '@/constants/brand'

export const metadata: Metadata = {
  title: BRAND_NAME,
  description: 'Trợ lý cuộc họp — ghi chú và tóm tắt cục bộ',
}
